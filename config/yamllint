extends: default
rules:
  line-length:
    max: 280
    level: warning 
  comments: disable
  comments-indentation: disable
  document-start: disable # Disables the rule requiring a `---` at the start of the document`
